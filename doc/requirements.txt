# Pinned so CI builds are reproducible and match the pixi environment.
# To bump: update the pixi docs feature (rosetta_ws/pixi.toml), run
# `pixi run docs-build`, then copy the resolved versions here.
sphinx==9.1.0
myst-parser==5.1.0
sphinx_rtd_theme==3.1.0
sphinx-copybutton==0.5.2
